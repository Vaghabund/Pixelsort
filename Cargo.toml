[package]
name = "pixelsort"
version = "0.1.0"
edition = "2021"

[dependencies]
sdl2 = { version = "0.35", features = ["image"] }
rand = "0.8"
image = "0.24"
rayon = "1.8"

# Pi5 ARM64 optimizations
[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
